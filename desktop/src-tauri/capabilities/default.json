{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Termx window capability. Remote pages served by the local backend intentionally receive no Tauri IPC access.",
  "windows": ["main", "connect"],
  "permissions": ["core:default"]
}
